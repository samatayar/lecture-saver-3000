# 📚 Document Chat Assistant

تطبيق ويب تفاعلي مبني بـ Streamlit يتيح رفع ملفات PDF والدردشة مع محتواها باستخدام تقنية RAG (Retrieval-Augmented Generation).

## ✨ المميزات

- 📤 **رفع الملفات**: إمكانية رفع ملفات PDF متعددة
- 🔄 **معالجة تلقائية**: تحويل PDF إلى نصوص، تقسيمها، وإنشاء embeddings
- 💬 **دردشة ذكية**: طرح أسئلة بالعربية أو الإنجليزية والحصول على إجابات دقيقة
- 🔍 **مصادر موثوقة**: عرض المصادر والصفحات المرجعية لكل إجابة
- 💾 **قاعدة بيانات متجهة**: استخدام ChromaDB للبحث السريع والدقيق
- 🗄️ **إدارة المجموعات**: إنشاء مجموعات منفصلة للبيانات المختلفة
- 📥 **استيراد البيانات**: استيراد البيانات من ملفات JSON موجودة
- 🔄 **الدردشة مع البيانات الموجودة**: إمكانية الدردشة مع البيانات المحفوظة سابقاً بدون إعادة رفع

## 🚀 التشغيل

### المتطلبات الأساسية

- Python 3.8+
- حساب OpenRouter للحصول على API key
- حساب Hugging Face (اختياري للـ embeddings)

### إدارة قواعد البيانات

التطبيق يدعم إنشاء مجموعات منفصلة من البيانات لتنظيم المحتوى بشكل أفضل:

#### إنشاء مجموعة جديدة:
1. اذهب إلى قسم "🗄️ قواعد البيانات" في الشريط الجانبي
2. اضغط على "➕ إنشاء مجموعة جديدة"
3. أدخل اسم المجموعة واضغط "إنشاء"

#### اختيار مجموعة:
- استخدم قائمة "اختر مجموعة البيانات" للتبديل بين المجموعات المختلفة
- كل مجموعة تحتوي على بيانات منفصلة تماماً

#### استيراد البيانات:
- يمكن استيراد البيانات من ملفات JSON موجودة
- اذهب إلى "📥 استيراد من ملف JSON" في الشريط الجانبي
- حدد مسار الملف واسم المجموعة المستهدفة

#### إدارة المجموعات:
- عرض إحصائيات كل مجموعة (عدد القطع)
- عرض عينة من البيانات
- حذف مجموعات غير مطلوبة
- تحديث قائمة المجموعات

### التثبيت

1. **استنساخ المشروع**:
```bash
git clone <repository-url>
cd document-chat-assistant
```

2. **تثبيت المتطلبات**:
```bash
pip install -r requirements.txt
```

3. **إعداد مفاتيح API**:

   إنشاء ملف `secrets.toml` في مجلد `.streamlit`:
   ```toml
   [secrets]
   OPENROUTER_API_KEY = "sk-or-v1-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"
   HUGGINGFACE_TOKEN = "hf_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"
   ```

   أو تعيين متغيرات البيئة:
   ```bash
   export OPENROUTER_API_KEY="sk-or-v1-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"
   export HUGGINGFACE_TOKEN="hf_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"
   ```

4. **تشغيل التطبيق**:
```bash
streamlit run app.py
```

## 📖 كيفية الاستخدام

### الدردشة مع البيانات الموجودة مسبقاً

إذا كان لديك بيانات محفوظة في ChromaDB من جلسات سابقة:

1. **التطبيق سيبدأ تلقائياً** بالكشف عن المجموعات الموجودة
2. **اختر المجموعة المطلوبة** من قائمة "اختر مجموعة البيانات"
3. **ابدأ بالدردشة مباشرة** - لا حاجة لإعادة رفع الملفات

### رفع ومعالجة ملفات جديدة

1. **رفع الملفات**:
   - استخدم الشريط الجانبي لرفع ملفات PDF
   - يمكنك إنشاء مجموعة جديدة للملفات أو إضافتها لمجموعة موجودة
   - اضغط على "معالجة الملفات" لبدء المعالجة

2. **الدردشة**:
   - اكتب سؤالك في مربع النص
   - يمكنك السؤال بالعربية أو الإنجليزية
   - ستحصل على إجابة مع المصادر المرجعية

### استيراد البيانات من ملفات JSON

إذا كان لديك ملفات JSON تحتوي على embeddings محفوظة:

1. اذهب إلى "📥 استيراد من ملف JSON"
2. حدد مسار ملف JSON
3. اختر أو أنشئ مجموعة مستهدفة
4. اضغط "استيراد البيانات"

3. **إدارة الملفات**:
   - عرض قائمة الملفات المعالجة في الشريط الجانبي
   - معلومات عن عدد القطع المعالجة لكل ملف

## 🏗️ البنية التقنية

### خطوات المعالجة

1. **استخراج النص**: استخدام Docling لتحويل PDF إلى Markdown
2. **التقسيم**: تقسيم النص إلى قطع صغيرة باستخدام LangChain
3. **التضمين**: إنشاء متجهات باستخدام Sentence Transformers
4. **التخزين**: حفظ المتجهات في قاعدة بيانات ChromaDB
5. **البحث**: استرجاع الوثائق ذات الصلة بالسؤال
6. **التوليد**: إنشاء إجابة باستخدام OpenRouter API

### التقنيات المستخدمة

- **Streamlit**: واجهة المستخدم
- **Docling**: معالجة ملفات PDF
- **LangChain**: تقسيم النصوص
- **Sentence Transformers**: إنشاء المتجهات
- **ChromaDB**: قاعدة بيانات المتجهات
- **OpenRouter**: توليد الإجابات
- **OpenAI API**: نموذج اللغة

## ⚙️ التكوين

### إعدادات البحث

- **عدد النتائج**: عدد القطع المسترجعة (5-20)
- **حجم القطعة**: حجم النص المقسم (900 حرف)
- **التداخل**: مقدار التداخل بين القطع (120 حرف)

### متغيرات البيئة

```bash
# API Keys
OPENROUTER_API_KEY=sk-or-v1-...
HUGGINGFACE_TOKEN=hf_...

# ChromaDB Settings
CHROMA_DB_PATH=./chromaDB
COLLECTION_NAME=documents

# Model Settings
EMBEDDING_MODEL=sentence-transformers/distiluse-base-multilingual-cased-v2
LLM_MODEL=openai/gpt-4o-mini
```

## 🐛 حل المشاكل

### مشاكل شائعة

1. **خطأ في API Key**:
   - تأكد من صحة مفتاح OpenRouter API
   - تحقق من الرصيد المتاح

2. **خطأ في معالجة PDF**:
   - تأكد من أن الملف PDF سليم
   - جرب ملف PDF آخر

3. **بطء في المعالجة**:
   - قلل من حجم الملفات
   - انتظر انتهاء عملية التضمين

### تنظيف البيانات

```bash
# حذف قاعدة البيانات
rm -rf chromaDB/

# حذف الملفات المؤقتة
rm -rf temp_*/
```

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح تحت رخصة MIT.

## 🤝 المساهمة

نرحب بالمساهمات! يرجى:

1. إنشاء Fork للمشروع
2. إنشاء branch جديد للمميزة
3. إرسال Pull Request

## 📞 الدعم

للحصول على المساعدة أو الإبلاغ عن مشاكل:
- إنشاء Issue في GitHub
- مراجعة الـ logs في وحدة التحكم